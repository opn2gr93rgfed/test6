# Smart Dynamic Provider - Инструкция по использованию

## 🎉 Система динамического поиска ответов готова!

Провайдер `smart_dynamic` использует **моментальный O(1) поиск** ответов через hash-map (словарь Python). Это позволяет обрабатывать до 100+ вопросов в любом порядке без потери производительности.

---

## 🆕 ПОСЛЕДНИЕ ОБНОВЛЕНИЯ (2025-11-22)

### ✅ Исправлено: Дубликат "View my quotes" (popup button)

**Проблема:**
Скрипт успешно отвечал на 34 вопроса, но крашился при попытке кликнуть "View my quotes":
```
TimeoutError: Timeout 10000ms exceeded
```

**Причина:**
Кнопка кликалась дважды:
1. В вопросе "One final step" (как последнее действие)
2. В `with page.expect_popup()` блоке (как триггер popup)

После первого клика popup открывался, кнопка исчезала, второй клик падал с таймаутом.

**Решение:**
Парсер теперь автоматически:
- Обнаруживает дубликат кнопки в `with page.expect_popup()` блоке
- Удаляет клик из последнего вопроса
- Оставляет клик только внутри `with` блока
- Добавляет `.click()` если Playwright Recorder его пропустил

**Тест:** `test_popup_duplicate.py` - все проверки пройдены ✅

### ✅ Улучшено: #optional команда с понятным логированием

**Обновление:**
Команда `#optional` теперь генерирует понятные сообщения:

```python
#optional
page.get_by_role("heading", name="How did you hear about us?").click()
page.get_by_role("button", name="Not Now").click()
```

**Генерируется код:**
```python
# Optional element (may not be present)
print('[OPTIONAL] Trying optional element...', flush=True)
try:
    page.get_by_role("heading", name="How did you hear about us?").click()
    print('[OPTIONAL] [OK] Element found and clicked', flush=True)
except PlaywrightTimeout:
    print('[OPTIONAL] [SKIP] Element not found (this is OK)', flush=True)
    pass
```

**Преимущества:**
- Понятно когда элемент опциональный
- Четкое сообщение "[SKIP]" вместо "[WARNING]"
- Не вызывает беспокойства если элемент отсутствует

**Use case:** "Not Now" кнопка появляется в 90% случаев - теперь оба сценария обрабатываются корректно с понятным feedback.

---

## ✅ Проверка работоспособности

Все тесты пройдены:

```bash
python test_question_matching.py
```

**Результаты:**
- ✅ Точное совпадение - OK
- ✅ Нормализация текста (*, ?, !, .) - OK
- ✅ Fuzzy matching (substring) - OK
- ✅ Динамический порядок вопросов - OK
- ✅ Производительность O(1) - OK

---

## ⚠️ Исправление ошибки 403 (API Token)

### Проблема

Если вы видите ошибки:

```
[PROFILE] [ERROR] Ошибка API: 403
[MAIN] [ERROR] Итерация X завершена с ошибкой
```

Это означает что используется **фейковый API токен** `test_token_12345`.

### Решение

#### 1. Получить реальный API токен из Octobrowser

1. Открыть Octobrowser
2. Перейти в **Настройки** (Settings)
3. Вкладка **"Дополнительно"** (Advanced)
4. Скопировать **API Token**

Токен выглядит примерно так: `octo_abc123def456ghi789...`

#### 2. Обновить конфигурацию в тесте

Открыть `test_smart_dynamic_provider.py`:

```python
# Конфигурация
CONFIG = {
    'api_token': 'ВАШ_РЕАЛЬНЫЙ_ТОКЕН_ЗДЕСЬ',  # ← Заменить!
    'proxy': {
        'enabled': False
    },
    'proxy_list': {
        'proxies': [],
        'rotation_mode': 'random'
    },
    'profile': {
        'fingerprint': {'os': 'win'},
        'tags': ['auto-test']
    },
    'threads_count': 1,  # Начать с 1 потока
    'network_capture_patterns': [],
    'simulate_typing': True,
    'typing_delay': 100
}
```

#### 3. Запустить тест снова

```bash
python test_smart_dynamic_provider.py
```

Теперь профили должны создаваться и запускаться.

---

## 📊 Как работает динамический поиск

### 1. Парсинг user_code

Генератор извлекает вопросы из Playwright Recorder кода:

```python
page.get_by_role("heading", name="Are you currently insured?").click()  # ← МАРКЕР вопроса
page.get_by_role("button", name="No").click()  # ← Действие (ответ)
```

**Важно:** `.click()` на heading - это НЕ действие, а просто **обозначение вопроса** в записи.

### 2. Создание словаря QUESTIONS_POOL

```python
QUESTIONS_POOL = {
    "Are you currently insured?": {
        "actions": [
            {"type": "button_click", "name": "No"}
        ]
    },
    "What's your gender?": {
        "actions": [
            {"type": "button_click", "name": "Female"}
        ]
    },
    # ... до 100+ вопросов
}
```

### 3. Моментальный поиск на странице

```python
def answer_questions(page, data_row, max_questions=100):
    """
    Находит все heading на странице и мгновенно ищет ответы в словаре
    """
    # Получаем все вопросы на странице
    headings = page.get_by_role("heading").all()

    for heading in headings:
        question_text = heading.inner_text()

        # O(1) поиск в словаре с fuzzy matching
        matched_key = find_question_in_pool(question_text, QUESTIONS_POOL)

        if matched_key:
            # Выполняем действия для найденного вопроса
            execute_actions(...)
```

### 4. Fuzzy Matching (нечеткое совпадение)

Система умеет находить вопросы даже если:
- Есть лишние символы: `"Are you insured? *"` → `"Are you insured?"`
- Нет знаков препинания: `"What's your gender"` → `"What's your gender?"`
- Разная капитализация: `"WHAT'S YOUR GENDER?"` → `"What's your gender?"`

**Три уровня поиска:**
1. Точное совпадение
2. Нормализованное (без `*`, `?`, `!`, `.`)
3. Substring matching (>70% совпадения длины)

---

## 🚀 Преимущества Smart Dynamic

| Характеристика | Smart No API | **Smart Dynamic** |
|---------------|--------------|-------------------|
| Порядок вопросов | Последовательный | **Любой** |
| Количество вопросов | До ~20 | **До 100+** |
| Скорость поиска | O(n) линейная | **O(1) моментально** |
| Fuzzy matching | Нет | **Да** |
| Производительность | Медленнее при >20 | **Всегда быстро** |

---

## 📝 Пример использования

### User Code (из Playwright Recorder)

```python
page.goto("https://www.mytest.com/")
page.get_by_role("textbox", name="Enter your ZIP code").fill("12345")
page.get_by_role("button", name="See My Quotes").click()

# Вопросы (могут появиться в любом порядке)
page.get_by_role("heading", name="Are you currently insured?").click()
page.get_by_role("button", name="No").click()

page.get_by_role("heading", name="What's your gender?").click()
page.get_by_role("button", name="Female").click()

page.get_by_role("heading", name="Do you own or rent?").click()
page.get_by_role("button", name="Own").click()
```

### Что генерирует Smart Dynamic

```python
# Словарь создается автоматически
QUESTIONS_POOL = {
    "Are you currently insured?": {...},
    "What's your gender?": {...},
    "Do you own or rent?": {...}
}

# Функция answer_questions() ищет вопросы в любом порядке
def answer_questions(page, data_row, max_questions=100):
    # Находит все heading на странице
    # Для каждого ищет ответ в словаре (моментально!)
    # Выполняет действия
```

---

## 🔧 Настройки

### Threads Count (потоки)

```python
'threads_count': 1  # Начать с 1, затем увеличить до 3-5
```

**Рекомендации:**
- 1 поток - для отладки
- 3-5 потоков - для production
- >10 потоков - может вызвать rate limiting на API

### Proxy Rotation

```python
'proxy_list': {
    'proxies': [
        'http://user:pass@ip:port',
        'http://user:pass@ip:port',
    ],
    'rotation_mode': 'random'  # или 'sequential'
}
```

### Simulate Typing (имитация ввода)

```python
'simulate_typing': True,   # Медленный ввод (как человек)
'typing_delay': 100        # Задержка между символами (мс)
```

---

## 🐛 Отладка

### Включить DEBUG режим

В сгенерированном скрипте найти:

```python
# В функции answer_questions()
matched_key = find_question_in_pool(question_text, QUESTIONS_POOL, debug=True)
```

Установить `debug=True` чтобы видеть:
- Какие вопросы ищутся
- Какие вопросы в пуле
- Процесс fuzzy matching

### Проверить парсинг вопросов

```bash
python test_smart_dynamic_provider.py
```

Смотреть секцию:

```
[4] Проверка парсинга вопросов...
  ✓ Найдено вопросов: 8

  Примеры вопросов:
    1. 'Are you currently insured?'
       Действий: 1
    2. 'What's your gender?'
       Действий: 1
```

---

## 📚 Файлы провайдера

```
src/providers/smart_dynamic/
├── __init__.py           # Инициализация
├── generator.py          # Генератор скриптов (основной файл)
└── runner.py             # Раннер без API зависимостей

Тесты:
├── test_smart_dynamic_provider.py      # Полный тест с API
├── test_question_matching.py           # Тест логики без API ✅
└── test_generated_dynamic_script.py    # Сгенерированный скрипт (пример)
```

---

## 🎯 Следующие шаги

1. ✅ Получить реальный API токен из Octobrowser
2. ✅ Обновить `test_smart_dynamic_provider.py` с токеном
3. ✅ Запустить `python test_smart_dynamic_provider.py`
4. ✅ Проверить что профили создаются
5. ✅ Проверить что вопросы находятся на странице
6. ✅ Увеличить threads_count до 3-5 для production

---

## ❓ FAQ

### Q: Что делать если вопрос не найден?

**A:** Включить debug режим:
```python
matched_key = find_question_in_pool(question_text, QUESTIONS_POOL, debug=True)
```

Система покажет:
- Текст искомого вопроса
- Первые 5 вопросов в пуле
- Нормализованные версии

### Q: Можно ли использовать с другими сайтами?

**A:** Да! Просто:
1. Записать новый user_code через Playwright Recorder
2. Обновить `USER_CODE` в тесте
3. Запустить генератор

### Q: Работает ли с 100+ вопросами?

**A:** Да! Тест производительности показал:
```
Пул: 100 вопросов
Время поиска: 0.001 мс
```

Поиск O(1) независимо от количества вопросов.

### Q: Нужно ли указывать порядок вопросов?

**A:** Нет! Это главное преимущество. Вопросы могут идти:
- 1, 2, 3
- 3, 1, 2
- 2, 3, 1
- Любой порядок

Система найдет их автоматически.

---

## 💡 Совет

Если нужно протестировать логику поиска БЕЗ браузера и API:

```bash
python test_question_matching.py
```

Этот тест проверяет только алгоритм fuzzy matching без зависимостей.

---

**Создано:** 2025-11-21
**Провайдер:** smart_dynamic
**Статус:** ✅ Готов к использованию (нужен реальный API токен)
