# PROVIDER DEVELOPMENT GUIDE
## Критические правила разработки провайдеров для auto2tesst

Этот документ содержит **обязательные к выполнению** правила при создании новых провайдеров. Несоблюдение этих правил приведет к критическим ошибкам времени выполнения.

---

## ⚠️ КРИТИЧЕСКАЯ ПРОБЛЕМА #1: Playwright Recorder Code Parsing

### Проблема

**Playwright Recorder генерирует STANDALONE скрипты**, которые запускают собственный браузер через `launch()`.

**Наши провайдеры подключаются к УЖЕ ЗАПУЩЕННОМУ браузеру** через CDP (Chrome DevTools Protocol) в Octobrowser.

**Если вставить код Playwright Recorder "как есть"**, получим:
- ❌ Двойной вызов `sync_playwright()`
- ❌ Попытку запустить второй браузер через `launch()`
- ❌ Конфликт asyncio event loop
- ❌ `NotImplementedError` в asyncio
- ❌ Профиль открывается, но автоматизация не запускается

### Корневая причина

```python
# НЕПРАВИЛЬНО: Вставка кода Playwright Recorder как есть
def run_iteration(page, data_row, iteration_number):
    # page уже подключен к Octobrowser через CDP!

    # Но вставленный код делает:
    import re
    from playwright.sync_api import sync_playwright

    def run(playwright):
        browser = playwright.chromium.launch()  # ← ВТОРОЙ браузер!
        context = browser.new_context()
        page = context.new_page()  # ← Игнорирует переданный page!
        # ... действия ...
        browser.close()

    with sync_playwright() as playwright:  # ← ВТОРОЙ вызов!
        run(playwright)
```

### ✅ Правильное решение

**ОБЯЗАТЕЛЬНО парсить пользовательский код и удалять весь boilerplate:**

```python
def _clean_user_code(self, user_code: str) -> str:
    """
    Очистить код от boilerplate Playwright Recorder

    УДАЛЯЕТ:
    - import statements (re, playwright imports)
    - def run(playwright) wrapper
    - browser.launch(), new_context(), new_page()
    - browser.close(), context.close()
    - with sync_playwright() wrapper
    - run(playwright) вызовы
    - separator comments (# -----)

    ОСТАВЛЯЕТ ТОЛЬКО:
    - page.goto()
    - page.get_by_role()
    - page.fill()
    - page.click()
    - page.wait_for_*()
    - И другие ДЕЙСТВИЯ пользователя
    """
    # Реализация см. src/providers/smart_no_api/generator.py:458-544
```

**Правильный run_iteration():**

```python
def run_iteration(page, data_row, iteration_number):
    """
    Args:
        page: УЖЕ подключенный Playwright page через CDP!
    """
    try:
        # Вставляем ТОЛЬКО очищенные действия:
        page.goto("https://example.com/")
        page.get_by_role("textbox", name="Name").fill(data_row["Field 1"])
        page.get_by_role("button", name="Submit").click()
        # ... только действия, БЕЗ launch/close!

        return True
    except Exception as e:
        print(f"Error: {e}")
        return False
```

### Референсная реализация

**Файл:** `src/providers/smart_no_api/generator.py`

**Методы:**
- `_clean_user_code()` (строки 458-544) - парсинг и очистка
- `_generate_main_iteration()` (строки 546-583) - использование очищенного кода

**Ключевые строки:**
```python
# generator.py:548
cleaned_code = self._clean_user_code(user_code)

# generator.py:571
{self._indent_code(cleaned_code, 8)}  # Вставляем ТОЛЬКО действия!
```

---

## ⚠️ КРИТИЧЕСКАЯ ПРОБЛЕМА #2: Local API Profile Start Method

### Проблема

Octobrowser Local API для запуска профилей работает НЕ так, как ожидается из REST conventions.

**ЕДИНСТВЕННО ПРАВИЛЬНЫЙ способ:**

```python
# ✅ ПРАВИЛЬНО (POST с body)
response = requests.post(
    f"{LOCAL_API_URL}/profiles/start",  # НЕ /profiles/{uuid}/start!
    json={
        "uuid": profile_uuid,
        "debug_port": True,
        "headless": False,
        "only_local": True,
        "timeout": 120
    },
    timeout=120
)
```

**НЕПРАВИЛЬНЫЕ способы (вызывают 404):**

```python
# ❌ НЕПРАВИЛЬНО - GET вместо POST
requests.get(f"{LOCAL_API_URL}/profiles/{uuid}/start")

# ❌ НЕПРАВИЛЬНО - UUID в URL вместо body
requests.post(f"{LOCAL_API_URL}/profiles/{uuid}/start")

# ❌ НЕПРАВИЛЬНО - пустой body
requests.post(f"{LOCAL_API_URL}/profiles/start")
```

### Референсная реализация

**Файл:** `src/providers/smart_no_api/generator.py`
**Функция:** `start_profile()` (строки ~220-280)
**Также:** `src/gui/components/octo_api_tab.py` - test_start_profile()

**Обязательные warning комментарии:**
```python
# ============================================================
# ⚠️ КРИТИЧЕСКИ ВАЖНО: ЕДИНСТВЕННО ПРАВИЛЬНЫЙ СПОСОБ ЗАПУСКА ПРОФИЛЯ!
# ============================================================
# Метод: POST (НЕ GET!)
# URL: /profiles/start (НЕ /profiles/{uuid}/start!)
# Body: JSON с uuid и параметрами
#
# ❌ НЕПРАВИЛЬНО: GET /profiles/{uuid}/start
# ✅ ПРАВИЛЬНО: POST /profiles/start с body
# ============================================================
```

---

## ⚠️ КРИТИЧЕСКАЯ ПРОБЛЕМА #3: Heading Clicks Не Работают

### Проблема

**Playwright Recorder записывает клики на заголовках** для подтверждения навигации:

```python
page.get_by_role("heading", name="What's your date of birth?").click()
```

**Проблемы с этим подходом:**
- ❌ Заголовки обычно НЕ интерактивные элементы
- ❌ `.click()` падает с таймаутом если heading не появился вовремя
- ❌ Таймаут по умолчанию 30 секунд - слишком долго
- ❌ Нет retry логики или альтернативных вариантов текста
- ❌ Скрипт зависает на неожиданных шагах

### Корневая причина

Генератор вставлял код **КАК ЕСТЬ**, не преобразуя heading clicks:

```python
def run_iteration(page, data_row, iteration_number):
    # Функция check_heading() ЕСТЬ в скрипте
    # Но НИКОГДА НЕ ИСПОЛЬЗУЕТСЯ!

    # Вместо этого вставляется код Recorder:
    page.get_by_role("heading", name="What's your date of birth?").click()
    # ← Если heading не появился - скрипт падает!
```

### ✅ Правильное решение

**ОБЯЗАТЕЛЬНО трансформировать heading clicks в вызовы check_heading():**

```python
def _clean_user_code(self, user_code: str) -> str:
    """
    Трансформирует:
    - page.get_by_role("heading", name="...").click() → check_heading(page, ["..."], timeout=15000)
    """
    import re

    # ...парсинг кода...

    # Transform heading clicks
    if 'get_by_role("heading"' in stripped or "get_by_role('heading'" in stripped:
        # Extract heading text using regex
        match = re.search(r'get_by_role\(["\']heading["\']\s*,\s*name=["\']([^"\']+)["\']', stripped)
        if match:
            heading_text = match.group(1)
            # Generate check_heading call with increased timeout
            transformed_line = f'check_heading(page, ["{heading_text}"], timeout=15000)'
            cleaned_lines.append(transformed_line)
            continue  # Skip original .click() line
```

**Правильная функция check_heading():**

```python
def check_heading(page, expected_texts, timeout=15000):
    """
    Проверка наличия заголовка с альтернативными текстами

    ВАЖНО: Используется для подтверждения загрузки страницы/шага.
    Автоматически вызывается вместо page.get_by_role("heading").click()
    """
    for text in expected_texts:
        try:
            heading = page.get_by_role("heading", name=text)
            heading.wait_for(state="visible", timeout=timeout)
            print(f"[CHECK_HEADING] [OK] Найден заголовок: {text}")
            return True
        except Exception as e:
            continue  # Try next alternative

    print(f"[CHECK_HEADING] [!] Заголовок не найден из списка: {expected_texts}")
    return False
```

### Преимущества

✅ **Таймаут 15 секунд** вместо 30 (быстрее падает при проблемах)
✅ **Поддержка альтернативных текстов** (можно передать список вариантов)
✅ **Убирается .click()** на неинтерактивном элементе
✅ **Четкие логи** с названием найденного/не найденного заголовка
✅ **Не падает скрипт** - можно обработать return False

### Референсная реализация

**Файл:** `src/providers/smart_no_api/generator.py`

**Методы:**
- `_clean_user_code()` (строки 503-524) - трансформация heading clicks
- `check_heading()` в _generate_helpers() (строки 375-402) - функция с таймаутом 15s

**Ключевая логика:**
```python
# generator.py:504-520
if 'get_by_role("heading"' in stripped:
    match = re.search(r'get_by_role\(["\']heading["\']\s*,\s*name=["\']([^"\']+)["\']', stripped)
    if match:
        heading_text = match.group(1)
        transformed_line = f'check_heading(page, ["{heading_text}"], timeout=15000)'
```

---

## 📋 Чек-лист для новых провайдеров

При создании нового провайдера в `src/providers/<provider_name>/generator.py`:

### 1. Парсинг пользовательского кода

- [ ] Реализован метод `_clean_user_code(user_code: str) -> str`
- [ ] Удаляются import statements
- [ ] Удаляется `def run(playwright)` wrapper
- [ ] Удаляется `browser.launch()`, `new_context()`, `new_page()`
- [ ] Удаляется `browser.close()`, `context.close()`
- [ ] Удаляется `with sync_playwright()` wrapper
- [ ] Удаляется вызов `run(playwright)`
- [ ] **ТРАНСФОРМИРУЮТСЯ heading clicks** в `check_heading()` вызовы
- [ ] Используется regex для извлечения текста заголовков
- [ ] Timeout для heading установлен 15000ms
- [ ] Сохраняются ТОЛЬКО действия пользователя (page.*)
- [ ] Корректно обрабатывается indentation

### 2. Метод run_iteration()

- [ ] Принимает параметр `page` (уже подключенный через CDP)
- [ ] НЕ вызывает `sync_playwright()` внутри функции
- [ ] НЕ вызывает `browser.launch()`
- [ ] НЕ создает новый context/page
- [ ] Использует переданный `page` объект напрямую
- [ ] Вставляет только очищенный код через `_clean_user_code()`

### 3. Profile Start (если используется Octobrowser)

- [ ] Используется **POST** метод
- [ ] URL: `/profiles/start` (НЕ `/profiles/{uuid}/start`)
- [ ] UUID передается в JSON body, НЕ в URL
- [ ] Добавлены warning комментарии о правильном методе
- [ ] Timeout увеличен до 120s
- [ ] Реализована retry логика с exponential backoff

### 4. Imports в генерируемом скрипте

- [ ] НЕ импортируется `asyncio`
- [ ] НЕ импортируется `sys` (если не нужен для других целей)
- [ ] НЕ добавляется код манипуляции event loop
- [ ] Импортируется только необходимое:
  - `requests`
  - `playwright.sync_api.sync_playwright`
  - `csv`, `time`, `typing`

### 5. Архитектура main()

- [ ] `sync_playwright()` вызывается ОДИН РАЗ в main()
- [ ] Подключение через CDP: `connect_over_cdp(debug_url)`
- [ ] Page передается в `run_iteration()` как параметр
- [ ] Нет вложенных вызовов `sync_playwright()`

---

## 🔍 Отладка проблем

### Симптом: "NotImplementedError" в asyncio

**Причина:** Двойной вызов `sync_playwright()`

**Решение:** Проверить, что `_clean_user_code()` удаляет все boilerplate

**Проверка:**
```python
# Временно добавить print для отладки:
def _generate_main_iteration(self, user_code: str) -> str:
    cleaned_code = self._clean_user_code(user_code)
    print("=" * 60)
    print("ORIGINAL CODE:")
    print(user_code)
    print("=" * 60)
    print("CLEANED CODE:")
    print(cleaned_code)
    print("=" * 60)
```

### Симптом: Profile start возвращает 404

**Причина:** Неправильный метод или URL

**Решение:** Использовать референсную реализацию из `smart_no_api`

**Проверка:**
```python
# Должно быть:
POST /profiles/start
Body: {"uuid": "...", "debug_port": true, ...}

# НЕ должно быть:
GET /profiles/{uuid}/start
```

### Симптом: Профиль открывается, но ничего не происходит

**Причина 1:** Код не очищен, пытается запустить новый браузер

**Причина 2:** `run_iteration()` не использует переданный `page`

**Решение:** Проверить generated script, убедиться что внутри `run_iteration()` нет `with sync_playwright()`

### Симптом: Скрипт зависает/замирает на неожиданном шаге

**Причина:** Heading clicks не трансформированы в `check_heading()` вызовы

**Признаки:**
- Профиль открылся ✅
- Несколько действий выполнено ✅
- Скрипт застрял на определенном шаге ❌
- В логах нет `[CHECK_HEADING]` сообщений ❌
- Скрипт ждет 30 секунд и падает с timeout ❌

**Решение:**
1. Проверить generated script - должны быть вызовы `check_heading()`
2. Убедиться что `page.get_by_role("heading").click()` НЕТ в коде
3. Проверить что timeout для check_heading = 15000ms
4. Перегенерировать скрипт с обновленным парсером

**Проверка сгенерированного кода:**
```python
# В run_iteration() ДОЛЖНО быть:
check_heading(page, ["What's your date of birth?"], timeout=15000)

# НЕ должно быть:
page.get_by_role("heading", name="What's your date of birth?").click()
```

**Отладка:**
```python
# В генераторе временно добавить:
def _clean_user_code(self, user_code: str) -> str:
    # ... код парсинга ...

    # Debug: показать трансформации
    if 'get_by_role("heading"' in stripped:
        print(f"[DEBUG] TRANSFORM: {stripped}")
        # ... трансформация ...
        print(f"[DEBUG] RESULT: {transformed_line}")
```

---

## 📚 Референсные файлы

При создании нового провайдера **ОБЯЗАТЕЛЬНО** изучить эти файлы:

1. **`src/providers/smart_no_api/generator.py`** - эталонная реализация
   - Правильный парсинг кода
   - Правильная архитектура main()/run_iteration()
   - Все критические комментарии и warnings

2. **`src/gui/components/octo_api_tab.py`** - test функции
   - Правильный метод start profile
   - Retry логика
   - Error handling

3. **Этот документ** - перед началом разработки нового провайдера

---

## ✅ Финальная проверка перед commit

Перед коммитом нового провайдера:

1. Сгенерировать тестовый скрипт
2. Проверить, что в `run_iteration()` нет `sync_playwright()`
3. Проверить, что нет `browser.launch()`
4. **Проверить, что heading clicks преобразованы в `check_heading()` вызовы**
5. Проверить, что `check_heading()` имеет таймаут 15000ms
6. Запустить скрипт с 1 строкой CSV
7. Убедиться, что профиль запускается И автоматизация выполняется
8. Проверить логи - должны быть `[CHECK_HEADING] [OK]` сообщения
9. Проверить логи - не должно быть NotImplementedError или 404
10. Успешно? Commit с детальным описанием того, как провайдер решает проблему

---

## 📝 История изменений

- **2025-11-19 (начальная версия)**: Создан документ после обнаружения критической проблемы с парсингом Playwright Recorder кода
  - Commit: `829f5c7` - fix(generator): parse user code to extract only actions
  - Commit: `c8cec9c` - fix(generator): close asyncio event loop (отменен, т.к. не та проблема)
  - Commit: `1dc1a93` - fix(api): use correct POST method for Local API profile start
  - Commit: `c9fcf76` - docs(api): add critical warning comments for profile start method

- **2025-11-19 (обновление)**: Добавлена КРИТИЧЕСКАЯ ПРОБЛЕМА #3 - Heading Clicks
  - Обнаружено: `check_heading()` функция генерировалась, но НИКОГДА не использовалась
  - Причина: парсер не трансформировал `page.get_by_role("heading").click()` в вызовы `check_heading()`
  - Решение: добавлена regex трансформация heading clicks в парсер
  - Увеличен timeout с 5s до 15s для более стабильной работы
  - Скрипты теперь НЕ зависают на неожиданных шагах
  - Обновлен чек-лист: добавлены 3 пункта про трансформацию headings
  - Обновлена финальная проверка: добавлены 2 пункта про check_heading

---

**ВАЖНО:** Этот документ является частью критической документации проекта. При обнаружении новых проблем - обновлять этот файл.

**ИСПОЛЬЗОВАНИЕ:** При разработке нового провайдера - сначала прочитать этот файл полностью, затем начинать разработку.
