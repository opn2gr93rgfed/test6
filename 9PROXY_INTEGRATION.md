# 9Proxy API Integration для auto2tesst

## 📋 Обзор

Интеграция 9Proxy API добавляет функционал динамической ротации прокси в приложение auto2tesst v3. Это позволяет автоматически менять прокси при каждой новой итерации автоматизации, что значительно повышает стабильность и снижает риск блокировок.

## 🎯 Основные возможности

- ✅ Получение списка прокси через 9Proxy API (`/api/proxy`, `/api/today_list`)
- ✅ Фильтрация по стране, штату, городу, ISP, типу плана
- ✅ Динамическая ротация (sequential/random)
- ✅ Автоматическая ротация при каждой итерации
- ✅ **Многопоточная поддержка** - каждый поток использует свой порт (6001, 6002, ...)
- ✅ **Изолированная ротация IP** - независимая ротация для каждого потока через `/api/forward`
- ✅ Проверка доступности прокси
- ✅ Skip offline proxies
- ✅ GUI интерфейс для настройки
- ✅ Сохранение/загрузка настроек в config.json

## 📦 Структура файлов

```
auto2tesst/
├── src/
│   ├── utils/
│   │   └── proxy_manager.py         # 🔥 Новый модуль - NineProxyManager
│   └── gui/
│       └── components/
│           └── proxy_tab.py          # Модифицирован - добавлена секция 9Proxy
└── config.json                       # Сохранение настроек 9Proxy
```

## 🚀 Использование

### 1. Запуск 9Proxy API

Убедитесь, что 9Proxy запущен и API доступен:

```bash
# По умолчанию API доступен на http://localhost:50000
# Проверьте в настройках 9Proxy
```

### 2. Настройка через GUI

1. Откройте приложение auto2tesst v3
2. Перейдите на вкладку **🌐 Proxies**
3. Найдите секцию **🌐 9Proxy API Dynamic Rotation**
4. Включите переключатель **Enable 9Proxy**
5. Настройте параметры:
   - **API Base URL**: `http://localhost:50000` (по умолчанию)
   - **Country**: Выберите код страны (US, VN, RU, DE, FR, GB, и т.д.)
   - **City**: Опционально - фильтр по городу
   - **ISP**: Опционально - фильтр по провайдеру
   - **Plan**: `all`, `premium` или `free`
   - **Count**: Количество прокси для загрузки (1-100)
   - **Only Today's Proxies**: Загружать только сегодняшние прокси

6. Нажмите **🔍 Test Connection** для проверки связи
7. Нажмите **📥 Fetch Proxies** для загрузки списка прокси
8. Нажмите **💾 Save Settings** для сохранения настроек

### 3. Настройки ротации

- **Auto-rotate on each iteration**: Автоматическая смена прокси при каждой новой итерации
- **Strategy**:
  - `sequential` - Последовательная ротация по списку
  - `random` - Случайный выбор прокси
- **Skip offline proxies**: Пропускать офлайн прокси

### 4. Ручная ротация

Нажмите **🔄 Rotate Now** для принудительной смены прокси без перезапуска скрипта.

## 🔥 Многопоточная ротация прокси

**Новая возможность v1.1.0**: Каждый поток получает свой выделенный порт для независимой ротации IP.

### Как это работает

1. **Автоматическая настройка портов**: При генерации скрипта система автоматически:
   - Назначает каждому потоку свой порт (6001, 6002, 6003, ...)
   - Вызывает `/api/forward` для каждого порта с уникальным прокси
   - Настраивает локальный SOCKS5 прокси на `127.0.0.1:600X`

2. **Изолированная ротация**: После завершения каждой итерации:
   - Вызывается `rotate_proxy_for_port(port)` для обновления IP
   - Используется `/api/forward` с новым proxy_id
   - Только этот поток получает новый IP (другие потоки не затронуты)

3. **Приоритет прокси** в сгенерированных скриптах:
   ```
   9Proxy (если включен) → Список прокси → Единый прокси
   ```

### Пример работы с 3 потоками

```
Thread 1 → Port 6001 → Proxy IP 1.2.3.4   (US, New York)
Thread 2 → Port 6002 → Proxy IP 5.6.7.8   (US, Los Angeles)
Thread 3 → Port 6003 → Proxy IP 9.10.11.12 (US, Chicago)

После итерации Thread 1:
Thread 1 → Port 6001 → Proxy IP 13.14.15.16 (US, Miami) ← Обновлено
Thread 2 → Port 6002 → Proxy IP 5.6.7.8     (US, Los Angeles) ← Без изменений
Thread 3 → Port 6003 → Proxy IP 9.10.11.12  (US, Chicago) ← Без изменений
```

### Важные детали

- **Базовый порт**: 6000 (можно изменить в proxy_manager.py)
- **Порты для потоков**: 6001, 6002, 6003, ... (base_port + thread_id)
- **Адрес прокси**: Все потоки используют `127.0.0.1:600X` (локальный SOCKS5)
- **Автоматическая ротация**: Управляется параметром `NINE_PROXY_AUTO_ROTATE`
- **Стратегия**: Можно выбрать sequential или random

## 💻 Программное использование

### Пример работы с NineProxyManager

```python
from src.utils.proxy_manager import NineProxyManager

# Инициализация
manager = NineProxyManager(api_base_url="http://localhost:50000")

# Тест подключения
success, message = manager.test_connection()
print(message)

# Загрузка прокси с фильтрами
success, message, proxies = manager.fetch_proxies(
    country="US",
    city="New York",
    plan="premium",
    num=20
)

if success:
    print(f"Загружено {len(proxies)} прокси")

    # Получить следующий прокси
    proxy = manager.get_next_proxy(strategy="sequential")
    print(f"Текущий прокси: {proxy['ip']}:{proxy.get('port', 8080)}")

    # Для использования с requests
    proxies_dict = manager.get_current_proxy_for_requests()
    # {'http': 'http://ip:port', 'https': 'http://ip:port'}

    # Для использования с Playwright
    playwright_proxy = manager.get_current_proxy_for_playwright()
    # {'server': 'http://ip:port'}
```

### Интеграция в скрипт автоматизации

```python
import requests
from src.utils.proxy_manager import NineProxyManager

# Инициализация менеджера
proxy_mgr = NineProxyManager()
proxy_mgr.fetch_proxies(country="US", num=10)

# В цикле автоматизации
for iteration in range(10):
    # Получить прокси для этой итерации
    proxy = proxy_mgr.get_next_proxy(strategy="sequential")

    if proxy:
        print(f"[ITERATION {iteration}] Using proxy: {proxy['ip']}")

        # Использовать прокси
        proxies = proxy_mgr.get_current_proxy_for_requests()
        response = requests.get("https://httpbin.org/ip", proxies=proxies, timeout=10)
        print(f"Your IP: {response.json()}")
    else:
        print(f"[ITERATION {iteration}] No proxy available")
```

### Многопоточная интеграция

```python
from src.utils.proxy_manager import NineProxyManager
import threading

# Инициализация менеджера
manager = NineProxyManager(api_base_url="http://localhost:50000")
manager.fetch_proxies(country="US", num=10)

# Настроить порты для 3 потоков
num_threads = 3
ports = manager.setup_ports_for_threads(num_threads)
print(f"Assigned ports: {ports}")  # [6001, 6002, 6003]

def thread_worker(thread_id, port):
    """Рабочая функция потока"""
    print(f"[Thread {thread_id}] Starting with port {port}")

    # Получить конфигурацию прокси для Octobrowser
    proxy_config = manager.get_proxy_config_for_port(port)
    print(f"[Thread {thread_id}] Proxy config: {proxy_config}")
    # {'type': 'socks5', 'host': '127.0.0.1', 'port': '6001', 'login': '', 'password': ''}

    # Выполнить работу...
    for iteration in range(5):
        print(f"[Thread {thread_id}] Iteration {iteration}")
        # ... автоматизация ...

        # Обновить IP для этого порта
        success, message, new_proxy = manager.rotate_port(port, strategy="sequential")
        if success:
            print(f"[Thread {thread_id}] IP rotated: {new_proxy['ip']}")

# Запустить потоки
threads = []
for i, port in enumerate(ports):
    thread = threading.Thread(target=thread_worker, args=(i+1, port))
    threads.append(thread)
    thread.start()

for thread in threads:
    thread.join()

print("All threads completed")
```

## 🔧 API Endpoints

### Основные endpoints 9Proxy

| Endpoint | Описание | Параметры |
|----------|----------|-----------|
| `GET /api/proxy` | Получить список прокси | `country`, `city`, `isp`, `plan`, `num`, `t` |
| `GET /api/today_list` | Получить только сегодняшние прокси | То же самое |
| `GET /api/forward` | Переадресация на конкретный прокси | `id`, `port`, `plan`, `t` |
| `GET /api/port_status` | Статус портов | `t` |
| `GET /api/set_port_range` | Установить диапазон портов | `start`, `num`, `t` |

### Пример ответа API

```json
{
  "error": false,
  "message": "text",
  "data": [
    {
      "id": "proxy_id_123",
      "city": "New York",
      "ip": "192.168.1.1",
      "country_code": "US",
      "is_online": true,
      "binding": null
    }
  ]
}
```

## ⚙️ Конфигурация

Настройки сохраняются в `config.json`:

```json
{
  "nine_proxy": {
    "enabled": true,
    "api_url": "http://localhost:50000",
    "filters": {
      "country": "US",
      "city": "",
      "isp": "",
      "plan": "all",
      "today": false,
      "num": 10
    },
    "rotation": {
      "auto_rotate": true,
      "strategy": "sequential",
      "skip_offline": true
    }
  }
}
```

## 🐛 Troubleshooting

### Проблема: "Connection Error"

**Решение:**
1. Убедитесь что 9Proxy запущен
2. Проверьте порт API (по умолчанию 50000)
3. Проверьте firewall

### Проблема: "No proxies found"

**Решение:**
1. Ослабьте фильтры (уберите city, ISP)
2. Попробуйте другую страну
3. Увеличьте `num` параметр

### Проблема: "Offline proxies"

**Решение:**
1. Включите **Skip offline proxies**
2. Загрузите больше прокси
3. Используйте `today_list` для свежих прокси

## 📊 Статистика

Получить статистику использования:

```python
stats = manager.get_stats()
print(stats)
# {
#     'total_proxies': 20,
#     'current_index': 5,
#     'total_requests': 100,
#     'failed_requests': 3,
#     'success_rate': '97.0%',
#     'last_fetch': '2025-11-25 14:30:00',
#     'current_proxy': '192.168.1.1:8080'
# }
```

## 🎯 Best Practices

1. **Загружайте достаточно прокси**: Рекомендуется `num=20-50` для стабильности
2. **Используйте фильтры**: Фильтруйте по стране для лучшей производительности
3. **Включите Skip Offline**: Пропускайте офлайн прокси автоматически
4. **Sequential для стабильности**: Используйте sequential rotation для предсказуемости
5. **Random для анонимности**: Используйте random rotation для повышения анонимности
6. **Today's proxies**: Используйте `today_list` для самых свежих прокси

## 🔐 Безопасность

- API работает локально (localhost:50000)
- Нет передачи credentials через интернет
- Все настройки хранятся локально в config.json
- Проверяйте is_online перед использованием прокси

## 🚀 Roadmap

- [ ] Автоматическая проверка прокси перед использованием
- [ ] Blacklist для плохих прокси
- [ ] Retry logic с автоматическим переключением на следующий прокси
- [ ] Статистика по странам и ISP
- [ ] Экспорт списка прокси в файл
- [ ] Интеграция с другими proxy провайдерами

## 📝 Changelog

### v1.1.0 (2025-11-25)
- ✅ **Многопоточная поддержка**: Каждый поток получает выделенный порт
- ✅ **Изолированная ротация IP**: Независимая ротация для каждого потока через `/api/forward`
- ✅ Добавлены методы:
  - `setup_ports_for_threads(num_threads)` - Подготовка портов
  - `assign_proxy_to_port(proxy, port, plan)` - Назначение прокси на порт
  - `rotate_port(port, strategy, plan)` - Обновление IP для порта
  - `get_proxy_config_for_port(port)` - Конфиг для Octobrowser
- ✅ Интеграция в `modern_main_window_v3.py`:
  - Автоматическая настройка портов при генерации скрипта
  - Передача конфигурации 9Proxy в генератор
- ✅ Интеграция в `generator.py`:
  - Генерация функций `rotate_proxy_for_port()` и `get_nine_proxy_for_thread()`
  - Приоритет прокси: 9Proxy → Список → Единый
  - Автоматическая ротация после каждой итерации
- ✅ Добавлено поле State для фильтрации по штатам
- ✅ Исправлен параметр `t` (теперь использует `t=2` вместо timestamp)

### v1.0.0 (2025-11-25)
- ✅ Создан модуль `proxy_manager.py` с классом `NineProxyManager`
- ✅ Добавлена GUI секция в `proxy_tab.py`
- ✅ Реализована загрузка прокси через API
- ✅ Реализована ротация (sequential/random)
- ✅ Реализовано сохранение/загрузка настроек
- ✅ Добавлена документация

## 📞 Поддержка

Если у вас возникли проблемы:

1. Проверьте документацию выше
2. Проверьте логи в консоли (все действия логируются с префиксом `[9PROXY]`)
3. Проверьте что 9Proxy запущен и API доступен
4. Откройте issue на GitHub

---

**Автор**: Claude
**Дата**: 2025-11-25
**Версия**: 1.1.0
